[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
```

**`backend/requirements.txt`** (add to end)
```
# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
httpx==0.25.2
